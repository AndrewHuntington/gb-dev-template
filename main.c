// To  build: shift + cmd + b (mac) or shift + ctrl + b (win)
// To build, save and debug: F5 (with Emulicious running)

#include <gb/gb.h>
#include <stdio.h>

void main()
{
    printf("Hello World!");
}